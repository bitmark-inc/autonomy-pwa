import { SearchResultsNamePipe } from './search-results-name.pipe';

describe('SearchResultsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchResultsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
