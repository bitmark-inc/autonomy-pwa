import { ReportScorePipe } from './report-score.pipe';

describe('ReportScorePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportScorePipe();
    expect(pipe).toBeTruthy();
  });
});
