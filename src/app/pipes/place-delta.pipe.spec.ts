import { PlaceDeltaPipe } from './place-delta.pipe';

describe('PlaceDeltaPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceDeltaPipe();
    expect(pipe).toBeTruthy();
  });
});
