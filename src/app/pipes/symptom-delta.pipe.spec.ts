import { SymptomDeltaPipe } from './symptom-delta.pipe';

describe('SymptomDeltaPipe', () => {
  it('create an instance', () => {
    const pipe = new SymptomDeltaPipe();
    expect(pipe).toBeTruthy();
  });
});
